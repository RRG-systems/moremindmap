# Local development - use Vite proxy
# Frontend calls /api/* (relative path)
# Vite proxies to https://moremindmap.com (defined in vite.config.js)
# This avoids CORS errors on localhost:5173
VITE_API_URL=/
GPT_RESCORING_ENABLED=true
