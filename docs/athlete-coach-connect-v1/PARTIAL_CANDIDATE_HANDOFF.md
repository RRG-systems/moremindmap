# ATHLETE Coach Connect V1 — candidate handoff, runtime gates still open

This is an isolated synthetic DarrenDemo candidate, **not** a Production release. Its base is Home Base V2 `da9889e79a4b37adc6f0904b189309098d6a2202` / tree `3b6922e9e8c2e5c0c0d43768737e00a9a3021d2d`. The prior candidate commit is `a3b1ec7652f9d976d201e239e0c7aeafc5af7af5`; the Coach Connect handoff delta is sealed separately. The latest read-only provider inspection in the coordinating Home Base task resolved `moremindmap.com` to Production `dpl_7AqXDnxrZutXViW5FDf1sHhXrQ5s` (READY). Re-resolve immediately before any release; if still current, that deployment is the rollback for this campaign.

## Scoped candidate behavior

- Box 04 retains Athlete Consulting Tool and adds ATHLETE Coach Connect as a separate choice. Both use the existing Leadership capability, same demo scope, and only the two fictional athletes Nia/Sofia. V2-off hides and denies the new choice. No new login, real-coach role, customer reader or store was created.
- Coach Connect opens a coach-first capture screen. Athlete selection locks while a note is dirty or a voice operation runs. The coach can type or transcribe a short voice clip to editable text, then must review the note and selected athlete before saving. New Coach Connect notes persist text only: no photo or audio attachment. Legacy Consulting capture remains unchanged.
- A server-persisted `coach_connect_box04_v1` channel plus `reviewed:true`, exact Coach Alex source/role, exact selected synthetic subject/MM/BOS/APA identity, same-scope bridge, text kind and zero attachments are all required for the next-opening handoff. Historical Consulting captures have no new channel marker and do not become pending handoffs.
- A qualifying note is withheld from mid-session CHAT/CLOSE model input, then included in that athlete's next `Start My Session` OPENING input even if older than the ordinary 50-message window. Only a successful opening durably writes a `coach_note_opened` event with note IDs, request ID and time. Failed or unknown openings leave the note pending; replay after a lost final acknowledgement returns the single saved receipt. The authenticated Home view uses existing protected message text and a metadata-only pending/delivered projection, with a source and no-plan-change disclosure.
- The model prompt asks MORE to attribute and discuss the coach observation alongside an accepted plan. A note is not verified fact, an instruction, approved learning, homework or accepted plan. OPENING cannot silently retire an existing draft or stage learning from model output. The state machine still requires the athlete to choose `Use this plan` and the coach to agree to any coach-owned steps. No BOS, APA, learning or plan is automatically changed.
- Voice transcription remains default-off behind `ATHLETE_COACH_CONNECT_VOICE_TRANSCRIPTION_ENABLED=true`, same-origin and protected by the existing synthetic Leadership, instructor actor and state-bound CSRF. It allows at most four new clips per 15 minutes per scope, one provider attempt with no automatic retry, and no saved audio. The source handles M4A/MP4, MP3, WebM and WAV with a 30-second/250-KB limit; hosted format/device proof remains open.

## Evidence and remaining release gates

The combined affected Athlete V1/V2, Leadership, Recruiting and Darren Library matrix passes **144/144**. Focused handoff tests cover authenticated capture-to-opening flow, cross-athlete and old-Consulting exclusion, an older-than-50 note, mid-session withholding, failed and uncertain openings, lost final acknowledgement, no automatic draft/learning write, and rendered authenticated pending/delivered UI. Scoped ESLint, `git diff --check` and the deployable build pass. These are offline/source results, not a live model, browser, provider or Production verdict. Two fictional M4A fixtures pass format/size validation; no paid transcription call has been made.

Before promotion, re-resolve exact Production and rollback; verify the server-only provider binding without exposing its value; deploy an unaliased candidate; run bounded synthetic real-model transcription and next-opening semantic checks for Nia and Sofia; complete authenticated Box 04 browser QA, protected product/regression and privacy/network checks; and verify rollback readiness. Do not move the alias if any gate fails. No customer or real athlete data, Subscription repair, youth/Lab, or other campaign is in scope.
