# Layer 3 Translation Contract Repair Report

Campaign: `MORE_CAMPAIGN_BOS_LAYER3_LANGUAGE_REPAIR_V1`

The Layer 3 contract now uses a surface-specific `headline` plus typed `blocks` shape instead of forcing five compliance fields onto every customer surface. The prompt describes communication goals rather than mandatory sentence stems.

The repair preserves every supplied claim ID and the Layer 2 evidence, confidence, sufficiency, provenance, classification, score, and rank contracts. Default customer copy rejects internal implementation language. Technical source material remains available outside the normal customer projection.

Versions:

- Translation: `bos_customer_translation_v2`
- Prompt: `bos_customer_translation_prompt_v2`
- Validator: `bos_customer_translation_validator_v2`
- Output variant: `premium-web-v2`
- Language mode: `recognition-first`

The validator also enforces allowed block types, claim-subset use, semantic invariants, unsupported-certainty rules, and cross-surface exact-copy repetition limits. Safe validator failure codes are exposed only in the orchestration receipt; a rejected translation still fails back to exact Layer 2.
