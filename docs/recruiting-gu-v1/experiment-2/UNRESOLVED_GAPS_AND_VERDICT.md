# Unresolved gaps and bounded verdict

## Blocking gap

The requested two-subject comparison is incomplete. The legitimate local Patricia path requires a read-only Redis binding. The accessible environment provides an empty Redis value.

Read-only Production diagnostics prove the underlying state exists:

- BOS: healthy current, compatibility A, 15/15 surfaces.
- BA: completeness PASS, compatible-prior realization available.

The public BA artifact route is not observationally pure: after serving the artifact it invokes Recruiting canonical reconciliation. Calling it would exceed the no-external-mutation boundary. No unsafe HTTP fallback, dossier copy, generated substitute, or synthetic Patricia was created.

Minimum resolution: provide the existing local experiment process a legitimate read-only Redis credential or a reviewed side-effect-free BOS/BA artifact reader. Then run the already-built fixed four-condition harness against Patricia without changing the experiment.

## Non-blocking gaps

- The clean checkpoint's synthetic Jordan fixture has four BOS raw answers but no separate raw BA answer fixture. BUSINESS still receives the complete governed synthetic Business Twin, but cannot prove BA first-party-answer materiality in the synthetic control.
- Optional GU compilation remains much slower than coaching (30,929 ms in the successful repair probe). Progressive coaching protects first usefulness, but visual latency remains a hardening target.
- One compiler candidate needed a block-kind repair. The existing validator contained it; repair rate for that probe was 1/2 attempts.
- Ranked BUSINESS context is still large (76,067 characters). It is far below an all-16-Bible dump and selects two authorities, but further purpose retrieval should be evaluated only after the Patricia comparison.

## Verdict

The architecture is validated and the synthetic ablation supports promotion of **coach first, GU second** as the leading Experiment 2 design. The full campaign cannot be declared complete because Patricia was not legitimately readable in the local environment.

`RECRUITING_GU_V1_EXPERIMENT_2_ARCHITECTURE_VALIDATED_SYNTHETIC_ABLATION_COMPLETE_PATRICIA_READ_ONLY_BINDING_BLOCKED_NO_PRODUCTION_AUTHORITY`
