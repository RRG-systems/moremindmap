# Recruiting GU V1 Experiment 2 — Founder Evidence Package

## Outcome

The central architecture works in real software: a distinct frontier coach can understand the governed state, return one useful insight/explanation/question in roughly 2.6–5.0 seconds, and leave the authored BOS or BA visible when a generated visual would not materially help. The prior one-call control took roughly 15–17 seconds and generated a multi-block environment on every turn.

The strongest synthetic condition is **split coach/compiler + purpose-ranked context + three DJ demonstrations**. It produced the clearest correction response, kept YOU BOS-only, and chose conversation instead of GU on all three fixed turns.

The campaign is not fully complete because the local experiment has no usable read-only Redis binding for Patricia. Current read-only Production diagnostics prove that Patricia's BOS is healthy/current with 15 surfaces and that the BA is complete as a compatible-prior realization. The only public BA artifact route also invokes Recruiting reconciliation, so this campaign did not call it. Patricia fails closed rather than copying canonical data or mutating external state.

## Evidence map

- `ARCHITECTURE_AND_AUTHORITY.md` — exact staged runtime and preserved boundaries.
- `CHANGED_FILES.md` — exact implementation, test, and evidence file inventory.
- `ABLATION_RESULTS.md` — four-condition comparison and interpretation.
- `LIVE_FRONTIER_EVIDENCE.md` — representative transcripts, context sizes, latency, and validation.
- `TEST_AND_REGRESSION_EVIDENCE.md` — focused, protected, full-suite, lint, build, and bundle results.
- `SCREENSHOT_INDEX.md` — synthetic browser proof and Patricia fail-closed proof.
- `UNRESOLVED_GAPS_AND_VERDICT.md` — exact blocker and bounded terminal verdict.
- `evidence/SANITIZED_ABLATION_RECEIPT.json` — machine-readable summary without raw prompts, provider payloads, or sensitive answers.

Historical Free Frontier Experiment 1 evidence remains byte-preserved under `../free-frontier-coaching-experiment/` and in local branch `codex/recruiting-gu-v1-free-frontier-evidence` at commit `a163867`. Experiment 2 runtime work began from clean promotion checkpoint `af1c1f3`; no Experiment 1 runtime behavior was merged.

No Production or Darren Demo deployment, push, merge, Stripe, email, entitlement, reminder, customer write, canonical write, Recruiting V1 mutation, or web search occurred.
