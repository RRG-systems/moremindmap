# Test and regression evidence

## Focused acceptance

Command:

```text
node --test test/recruitingGuV1.test.js test/recruitingV2.syntheticSharedSession.test.js
```

Result: **20/20 passed**.

The focused suite covers:

- one revision-bound Shared Business Session;
- exact synthetic reset;
- invitee and MORE-ID authority boundaries;
- stale-write refusal;
- no scripted speaker toggle;
- governed scenario assumptions;
- owner-controlled MORE-ID consent;
- YOU BOS-only scoping;
- YOUR BUSINESS fused context and the complete 16-authority Real Estate packet;
- exact Founder communication and progressive-understanding doctrine;
- raw server-only answers absent from browser results;
- disagreement and hypothesis lineage;
- protected-answer leakage failing closed before rendering/persistence;
- exact state binding with order-independent serialization;
- the preserved Campaign 2G synthetic/shared-session regression set.

## Lint

Changed implementation, test, and harness files were passed explicitly to ESLint. Result: **green, zero findings**.

## Build

`npm run build`: **green**.

- 182 modules transformed.
- Client assets emitted successfully.
- Existing large-chunk advisory remains; it is not a build failure and this experiment did not redesign bundling.

## Full repository comparison

A completed source-equivalent experiment-branch replay produced **902 passed / 20 failed / 922 total**. The 20 failures were all in the pre-existing Coach Connect private-runtime async-security cluster. An untouched archive of starting HEAD reproduced the same 20 named failures. The experiment therefore added **zero full-suite failures** in the completed comparison.

After that comparison, one focused state-binding serialization regression test and evidence files were added; no production source behavior changed. The final focused run is green at 20/20. A redundant final repository replay was interrupted only after it entered the same long async-security tail and reproduced the known failure pattern; it is not represented as a second completed full-suite result.

## Privacy and client-bundle checks

After the final build, the browser bundle is searched for the protected synthetic answer phrases. Their absence proves the server-only frontier context was not pulled into the Vite client graph. The manifest records the exact command verdict.

## Repository hygiene

- `git diff --check`: green.
- Generated Vite test cache removed from custody.
- No secret values recorded in source or evidence.
- No commit, push, merge, deployment, or external effect performed.
