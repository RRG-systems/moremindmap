import{hashCanonicalJson}from'../hashing.js';import{deepFreeze}from'../validation.js';
export const MODEL_PURPOSES=Object.freeze(['CONVERSATION_PLAN_PROPOSAL','EXTRACTION_PROPOSAL','EXPLANATION_PROPOSAL','EVIDENCE_REQUEST_PROPOSAL']);
export class ModelProvider{async propose(){throw Error('NOT_IMPLEMENTED')}}
export class DeterministicFixtureProvider extends ModelProvider{constructor(response){super();this.response=response;this.calls=0}async propose(request){this.calls+=1;return typeof this.response==='function'?this.response(request):this.response}}
const injection=/ignore (all|previous)|system prompt|developer message|reveal.*secret|<system>|role\s*:\s*system|authorize.*one move|execute.*action/i;
function minimalContext(context){return(context||[]).filter(x=>x.privacy_classification!=='COACH_SESSION_PRIVATE'&&x.scope_match===true).map(x=>({context_id:x.context_id,context_type:x.context_type,value:x.value,privacy_classification:x.privacy_classification})).sort((a,b)=>a.context_id.localeCompare(b.context_id));}
function validateProposal(output,scope){if(!output||!['RESPONSE_PLAN','EXTRACTION','EXPLANATION','EVIDENCE_REQUEST'].includes(output.proposal_type)||typeof output.payload!=='object'||Array.isArray(output.payload))return{valid:false,decision:'MALFORMED'};const forbidden=['canonical_event','authorization','consent','execute','share_coach_private','promote_learning','calibrated_prediction','causal_proof'];if(forbidden.some(k=>k in output.payload))return{valid:false,decision:'UNSAFE'};if(output.scope&&['tenant_id','profile_id','business_id'].some(k=>output.scope[k]!==scope[k]))return{valid:false,decision:'REJECTED'};return{valid:true,decision:output.human_review_required?'HUMAN_REVIEW_REQUIRED':output.clarification_required?'CLARIFICATION_REQUIRED':output.reduced_weight?'ACCEPTED_WITH_REDUCED_WEIGHT':'ACCEPTED_PROPOSAL'}}
export async function runGovernedModelMembrane({provider,feature_flags,request,context,retry_policy={max_attempts:1}}){if(feature_flags?.model_provider_enabled!==true)return deepFreeze({ok:false,decision:'PROVIDER_FAILURE',code:'MODEL_PROVIDER_DISABLED',provider_calls:0});if(!MODEL_PURPOSES.includes(request?.purpose)||!request?.tenant_id||!request.profile_id||!request.business_id)return deepFreeze({ok:false,decision:'REJECTED',code:'INVALID_MODEL_REQUEST'});const scoped=minimalContext(context);if(scoped.some(x=>typeof x.value==='string'&&injection.test(x.value))||injection.test(request.user_input||''))return deepFreeze({ok:false,decision:'UNSAFE',code:'PROMPT_INJECTION_DETECTED',provider_calls:0});const envelope={request_version:'1.0.0',request_id:request.request_id,trace_id:request.trace_id,tenant_id:request.tenant_id,profile_id:request.profile_id,business_id:request.business_id,purpose:request.purpose,prompt_id:request.prompt_id,prompt_version:request.prompt_version,output_schema_version:'1.0.0',privacy_policy_version:'model-context-v1',context:scoped,context_hash:hashCanonicalJson(scoped),user_input:request.user_input||null};let output,error;const attempts=Math.max(1,Math.min(2,retry_policy.max_attempts||1));for(let i=0;i<attempts;i++){try{output=await provider.propose(deepFreeze(envelope));error=null;break}catch(e){error=e;if(!['TIMEOUT','RATE_LIMIT','UNAVAILABLE'].includes(e.code))break}}if(error)return deepFreeze({ok:false,decision:'PROVIDER_FAILURE',code:['TIMEOUT','RATE_LIMIT','UNAVAILABLE'].includes(error.code)?error.code:'PROVIDER_ERROR',provider_calls:provider.calls??null});const validation=validateProposal(output,request);const receipt={receipt_id:`model_receipt_${hashCanonicalJson({envelope:{...envelope,user_input:null,context:envelope.context.map(x=>({...x,value:'[MINIMIZED]'}))},output_hash:hashCanonicalJson(output),decision:validation.decision}).slice(0,20)}`,decision:validation.decision,request_id:request.request_id,trace_id:request.trace_id,provider_id:output.provider_id||'DETERMINISTIC_FIXTURE',model_id:output.model_id||'DETERMINISTIC_FIXTURE',usage:output.usage||{input_units:0,output_units:0,cost:null},proposal_hash:hashCanonicalJson(output),privacy_safe:true};return deepFreeze({ok:validation.valid,decision:validation.decision,proposal:validation.valid?{...output,status:'PROPOSAL_ONLY',canonical_mutation_eligible:false}:null,receipt});}
